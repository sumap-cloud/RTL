# import time
# import sys
# import os
# from pathlib import Path

# # Add the Components directory to the Python path
# COMPONENTS_DIR = str(Path(__file__).parent.parent / 'Components')
# if COMPONENTS_DIR not in sys.path:
#     sys.path.insert(0, COMPONENTS_DIR)

# # Import POS actions
# from POSActions import (
#     launch_pos,
#     login_to_pos,
#     check_pos_ready,
#     add_item_by_ean
# )
# from POSActions.pos_popups import (
#     handle_loyalty_popup,
#     handle_donation_popup,
#     check_and_handle_popups
# )

# # Import tender functionality
# from Tenders import eft_payment

# def click_button_by_text(win, button_text, timeout=10):
#     start_time = time.time()
#     target_button = None
#     while time.time() - start_time < timeout:
#         buttons = win.descendants(control_type="Button")
#         for btn in buttons:
#             if (
#                 btn.window_text() == button_text
#                 and btn.is_visible()
#                 and btn.is_enabled()
#             ):
#                 target_button = btn
#                 break
#         if target_button:
#             break
#         time.sleep(1)
#     if target_button:
#         target_button.click_input()
#         time.sleep(0.5)
#         return True
#     else:
#         print(f"❌ '{button_text}' button not found within timeout.")
#         return False

# def main():
#     print("\n=== Starting Multiple Items with EFT Payment Flow ===\n")
    
#     # Step 1: Check if POS is ready, if not launch and login
#     is_ready, app = check_pos_ready()
    
#     if not is_ready:
#         print("ℹ️ POS needs to be launched or logged in")
#         if not launch_pos():
#             print("❌ Failed to launch POS")
#             return False
        
#         time.sleep(10)  # Wait for POS to initialize
        
#         if not login_to_pos("atmgr5", "abcd1234"):
#             print("❌ Failed to login to POS")
#             return False
            
#         is_ready, app = check_pos_ready()
#         if not is_ready:
#             print("❌ POS is not ready after login")
#             return False
    
#     # Get the main window
#     win = app.window(title_re=".*R10PosClient.*")
#     win.set_focus()
    
#     # Step 2: Add multiple items
#     items_to_add = [
#         "9300675084147",  # Example item 1
#         "9300675084147",  # Example item 2 (using same EAN for demo)
#         "9300675084147"   # Example item 3 (using same EAN for demo)
#     ]
    
#     def add_product_and_check_basket(win, ean):
#         """
#         Add a product using numpad and check basket
#         """
#         # Clear EAN field
#         print("\n=== Adding product ===")
#         print("Clearing EAN field...")
#         max_clear_attempts = 20
#         for _ in range(max_clear_attempts):
#             edit_fields = win.descendants(control_type="Edit")
#             cleared = True
#             for field in edit_fields:
#                 val = field.get_value() if hasattr(field, 'get_value') else field.window_text()
#                 if val.strip():
#                     cleared = False
#                     if click_button_by_text(win, "<<", timeout=2):
#                         time.sleep(0.2)
#             if cleared:
#                 print("✅ EAN field cleared.")
#                 break

#         # Enter EAN digits
#         print(f"Entering EAN: {ean}")
#         for digit in str(ean):
#             if not click_button_by_text(win, digit):
#                 print(f"❌ Failed to enter digit: {digit}")
#                 return False
#             time.sleep(0.2)

#         # Click OK to add product
#         if not click_button_by_text(win, "OK"):
#             print("❌ Failed to click OK button")
#             return False
#         print("✅ Product added successfully")

#         # Check basket contents
#         print("\n=== Checking basket ===")
#         time.sleep(1)
#         basket_controls = win.descendants(control_type="List")
#         if not basket_controls:
#             basket_controls = win.descendants(control_type="ListBox")
#         if not basket_controls:
#             basket_controls = win.descendants(control_type="ListView")

#         if basket_controls:
#             basket = basket_controls[0]
#             items = basket.descendants(control_type="ListItem")
#             if not items:
#                 items = basket.children()
            
#             print(f"Found {len(items)} item(s) in basket:")
#             promotion_found = False
#             for item in items:
#                 txt = item.window_text()
#                 print(f"- {txt}")
#                 if "promotion" in txt.lower() or "bonus" in txt.lower():
#                     promotion_found = True
#                     print(f"🎉 Promotion found: {txt}")
            
#             print("✅ Basket check completed.")
#             time.sleep(2)
#             return True
#         else:
#             print("❌ Could not find basket")
#             return False

#     print("\n=== Adding Multiple Items ===")
#     for idx, item in enumerate(items_to_add, 1):
#         print(f"\nAdding item {idx} of {len(items_to_add)}")
#         if not add_product_and_check_basket(win, item):
#             print(f"❌ Failed to add item {idx}")
#             return False
#         time.sleep(2)  # Wait between items

#     # After adding all items, capture final basket details
#     print("\n=== Capturing Final Basket Details ===")
#     basket_controls = win.descendants(control_type="List")
#     if not basket_controls:
#         basket_controls = win.descendants(control_type="ListBox")
#     if not basket_controls:
#         basket_controls = win.descendants(control_type="ListView")

#     if basket_controls:
#         basket = basket_controls[0]
#         items = basket.descendants(control_type="ListItem")
#         if not items:
#             items = basket.children()

#         all_items = []
#         total = 0.0
#         promo_total = 0.0

#         print("\nItems in basket:")
#         for idx, item in enumerate(items, 1):
#             name = item.window_text()
#             children = item.children()
#             price = None
#             quantity = None

#             for child in children:
#                 txt = child.window_text()
#                 if any(c in txt for c in ["$", ".", ","]):
#                     try:
#                         price = float(txt.replace("$", "").replace(",", "").strip())
#                     except ValueError:
#                         pass
#                 if txt.isdigit():
#                     quantity = int(txt)

#             print(f"  {idx}. Name: {name}, Quantity: {quantity if quantity else 'N/A'}, Amount: {price if price else 'N/A'}")
            
#             if price is not None:
#                 total += price
#                 if price < 0 or ("promotion" in name.lower() or "bonus" in name.lower() or "ff" in name.lower()):
#                     promo_total += price
            
#             if not any(word in name.lower() for word in ["promotion", "bonus", "ff"]):
#                 all_items.append((name, quantity, price))

#         print(f"\n� TOTAL Articles in basket (all pages): {len(all_items)}")
#         print(f"💰 TOTAL Basket amount across all pages: {total:.2f}")
#         if promo_total < 0:
#             print(f"🎯 Promotion/discount applied: {promo_total:.2f}")
#         else:
#             print("ℹ️ No promotion/discount applied.")
#         print(f"✅ Final total after promotion: {total:.2f}")

#         if promo_total < 0:
#             print("✅ Basket contains promotion items.")
#         else:
#             print("ℹ️ Basket contains items without promotions.")
#     else:
#         print("❌ Could not find basket")
#         return False

#     # Click OK to proceed after basket review
#     time.sleep(2)
#     if not click_button_by_text(win, "OK"):
#         print("❌ Failed to click final OK button")
#         return False
#     print("✅ Clicked final OK button")
#     time.sleep(2)  # Wait for any UI updates
    
#     # Final OK click to proceed
#     # time.sleep(2)
#     # if not click_button_by_text(win, "OK"):
#     #     print("❌ Failed to click final OK button")
#     #     return False
#     # print("✅ Clicked final OK button")
#     # time.sleep(2)

#     # Step 3: Handle loyalty popup and any other popups that appear
#     print("\n=== Handling Loyalty ===")
#     time.sleep(2)  # Wait for loyalty popup
#     if not handle_loyalty_popup(app, action="cancel"):
#         print("❌ Failed to handle loyalty popup")
#         return False
    
#     time.sleep(2)
    
#     # Handle any additional popups (donation, etc.)
#     print("\n=== Checking for Additional Popups ===")
#     if not handle_donation_popup(app):
#         print("ℹ️ No donation popup found or already handled")
    
#     # Check for any other random popups
#     if not check_and_handle_popups(app):
#         print("⚠️ Warning: Some popups might not have been handled")
    
#     time.sleep(2)
    
#     # Step 4: Complete with EFT payment
#     print("\n=== Completing with EFT Payment ===")
#     if not eft_payment(app):
#         print("❌ Failed to complete EFT payment")
#         return False
    
#     print("\n🎉 Multiple items with EFT payment flow completed successfully!")
    
#     # Final click to reset focus
#     time.sleep(2)
#     import random
#     rect = win.rectangle()
#     x = random.randint(rect.left + 50, rect.right - 50)
#     y = random.randint(rect.top + 50, rect.bottom - 50)
#     win.click_input(coords=(x, y))
    
#     return True

# if __name__ == "__main__":
#     try:
#         success = main()
#         if not success:
#             print("\n❌ Flow failed to complete successfully")
#             sys.exit(1)
#     except Exception as e:
#         print(f"\n❌ Unexpected error: {e}")
#         sys.exit(1)
